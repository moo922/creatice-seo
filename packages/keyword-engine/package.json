{
  "name": "@creative-seo/keyword-engine",
  "version": "0.1.0",
  "private": true,
  "description": "Keyword Intelligence engine: normalization, deduplication, semantic clustering, cannibalization detection and deterministic opportunity scoring",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint \"src/**/*.ts\"",
    "test": "jest --runInBand --config jest.unit.json"
  },
  "dependencies": {
    "@creative-seo/types": "^0.1.0",
    "@creative-seo/content": "^0.1.0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "jest": "^29.7.0",
    "reflect-metadata": "^0.2.2",
    "ts-jest": "^29.2.5",
    "typescript": "^5.6.3"
  }
}