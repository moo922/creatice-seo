{
  "name": "@creative-seo/gsc",
  "version": "0.1.0",
  "private": true,
  "description": "Google Search Console client, incremental sync and opportunity detection (shared by API and worker)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint \"src/**/*.ts\"",
    "test": "jest --runInBand --config jest.unit.json --passWithNoTests"
  },
  "dependencies": {
    "@creative-seo/types": "^0.1.0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "typescript": "^5.6.3"
  }
}
