{
  "name": "@creative-seo/orchestration",
  "version": "0.1.0",
  "private": true,
  "description": "n8n orchestration infrastructure: backend-owned jobs, webhook dispatch, idempotency, retries, timeouts and failure alerts",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint \"src/**/*.ts\"",
    "test": "jest --runInBand --config jest.unit.json"
  },
  "dependencies": {
    "@creative-seo/config": "^0.1.0",
    "@creative-seo/database": "^0.1.0",
    "@creative-seo/operations": "^0.1.0",
    "@creative-seo/types": "^0.1.0",
    "@nestjs/common": "^11.0.0",
    "@nestjs/core": "^11.0.0",
    "@nestjs/typeorm": "^11.0.0",
    "typeorm": "^0.3.20"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "^22.9.0",
    "jest": "^29.7.0",
    "reflect-metadata": "^0.2.2",
    "ts-jest": "^29.2.5",
    "typescript": "^5.6.3"
  }
}
